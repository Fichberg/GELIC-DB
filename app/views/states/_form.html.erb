<%= form_for(@state) do |f| %>
  <% if @state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@state.errors.count, "error") %> prohibited this state from being saved:</h2>

      <ul>
      <% @state.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Name" %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label "Country" %><br>
    <%= f.select :nome_pais, Country.all.map {|country| country.nome}%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
